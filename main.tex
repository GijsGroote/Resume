\documentclass{resume} % Use the custom resume.cls style

% define:
% * the language (dutch, enlish)
% * the use of a photo
% * extended or summarized content

\ifdefined\useLanguage
  % \useLanguage is defined
\else % set to english / dutch
  % \newcommand{\useLanguage}{english}
  \newcommand{\useLanguage}{dutch}
\fi
\newcommand{\lang}[2]{\ifnum\pdfstrcmp{\useLanguage}{dutch}=0 {#1}\else{#2}\fi}

\ifdefined\usephoto
  % \usephoto is defined :)
\else
  % set to true/false to show/hide photo
  \newcommand{\usephoto}{true}
\fi

\ifdefined\extended
  \newcommand{\extendedWorkExp}{\extended}
  \newcommand{\extendedEduExp}{\extended}
  \newcommand{\extendedOtherExp}{\extended}
\else
  % set to true/false to extend (or summarize)
  \newcommand{\extendedWorkExp}{true}
  \newcommand{\extendedEduExp}{true}
  \newcommand{\extendedOtherExp}{true}
\fi


\begin{document}

\input{header.tex}
\input{resume.tex}

\end{document}
